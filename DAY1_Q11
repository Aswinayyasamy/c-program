#include <stdio.h>

int main() {
    int num;
    char ch;

    
    scanf("%d", &num);
    scanf(" %c", &ch);  // Notice the space before %c to consume any leftover newline

  
    printf("Integer of %d is %c\n", num, (char)num);

    
    printf("Character of %c is %
